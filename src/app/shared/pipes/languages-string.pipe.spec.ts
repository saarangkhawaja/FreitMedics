import { LanguagesStringPipe } from './languages-string.pipe';

describe('LanguagesStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguagesStringPipe();
    expect(pipe).toBeTruthy();
  });
});
