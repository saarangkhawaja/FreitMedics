import { AgeConverterPipe } from './age-converter.pipe';

describe('AgeConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
